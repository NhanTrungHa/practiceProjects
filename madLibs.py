#! python3
# madLibs.py
# Usage: Reads a text file and replaces word types with user input.

# Open a file in write mode
# Create regexes for possible word types
# loop through each word and compare to regex
# if regex matches then prompt user
# after looping through entire text file print and save and close the file